/** Deprecated: LegacyPathBridge is no longer used. Kept temporarily to avoid import churn.
 *  It performs a no-op render to satisfy old imports until they are fully removed. */
export default function LegacyPathBridge() { return null; }
